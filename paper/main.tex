\documentclass{article}

% NeurIPS 2026 workshop style. Drop neurips_2026.sty beside this file (it ships
% with the official Overleaf template). Keep the anonymous default for
% submission: do NOT add [final] until the paper is accepted.
% Without that file the paper still compiles, in an approximation of the
% NeurIPS single-column layout, so the draft can be read before the official
% style is dropped in. Close but not exact -- do not judge page count by it.
\IfFileExists{neurips_2026.sty}{%
  \usepackage{neurips_2026}%
}{%
  \usepackage[letterpaper,margin=1.4in,top=1.2in,bottom=1.2in]{geometry}%
  \usepackage{natbib}%
}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}   % scalable fonts, required by microtype expansion
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsmath}
\usepackage{microtype}
\usepackage{xcolor}

% Every numeric result is generated from the pipeline by
% scripts/build_paper_tex.py. Nothing below is typed by hand.
\input{generated/results}

\title{Enforced Abstention Prevents Silent Errors in Biomedical Evidence
Pipelines}

\author{Anonymous Author(s)\\Affiliation withheld for review}

% No date on the title page. \maketitle otherwise stamps the compile date,
% which is both a deanonymisation hint and noise on a submitted draft.
\date{}

\begin{document}

\maketitle

\begin{abstract}
Pipelines that assemble public biomedical records into scientific claims are
evaluated on what they emit. We show that this measurement misses their
dominant failure mode. A \emph{silent error} is an output that is wrong,
well-formed, free of any exception, and indistinguishable downstream from a
correct result. We introduce \emph{silent-error ablation}, a protocol that
removes a pipeline's verification guards and counts what the unguarded
pipeline then emits over identical inputs. We apply it to a rare-disease drug
repurposing pipeline in which every stage verifies its input against a stated
precondition or declines it, and every decline is counted as an output. Over
\CorpusFetched{} clinical variant records for \NumGenes{} genes, removing
residue verification makes \GOneWrong{} of \GOneNaive{} emitted protein
sequences silently wrong (\GOneRate{}). Removing copy-number exclusion
attributes \GTwoWrong{} of \GTwoNaive{} records to genes they say nothing
about, moving \GTwoZeroed{} of \NumGenes{} genes from apparent support to
zero. We report the cost alongside the benefit: the sequence lane declines
\SeqDeclined{} of \GOneProcessed{} records. We also report two defects the
protocol found in our own instrumentation, including a rate we had overstated
by ten percentage points. Both had the failure shape the protocol targets, and
a mechanical check was what exposed each one.
\end{abstract}

\section{Introduction}

Computational drug repurposing produces rankings. Recent systems score
millions of drug--disease pairs \citep{txgnn}, and commercial databases sell
curated competitive landscapes. Evaluation of these systems concentrates on
the quality of what they emit. They rarely record which evidence produced a
given score, and they rarely offer a principled way to report that the
evidence was insufficient, so a confident guess and an auditable finding reach
the reader in the same form.

The consequence matters because of how these pipelines fail. Their dominant
failure mode is the \emph{silent error}: an output that is (i)~wrong,
(ii)~syntactically well-formed, (iii)~free of any exception or warning, and
(iv)~indistinguishable by a downstream consumer from a correct output. Silent
errors are the non-statistical analogue of hallucination, plausible and
confident at the point of use. They are also invisible to any evaluation that
scores only the outputs a pipeline chooses to produce, since by construction
the pipeline believes them.

Our claim concerns the refusals. A pipeline can be built so that each stage
verifies its input against a stated precondition or declines it, with every
decline counted and given a reason, and this discipline prevents a measurable
class of silent error. We make three contributions.

\begin{enumerate}
\item \textbf{Silent-error ablation}, an evaluation protocol for evidence
pipelines. We score the pipeline's refusals by removing each verification
guard and counting what the resulting pipeline emits over identical inputs.
Scoring its recommendations instead would require wet-lab validation, which we
have not performed.
\item A \textbf{verify-or-abstain architecture} in which a small set of
readable rules assigns every verdict, model output is excluded from those
decisions, and every record is content-addressed under a single Merkle root
that a reader re-derives client-side.
\item \textbf{Measurements} on \CorpusFetched{} real public records, reported
with their cost and their limits, including two defects the protocol exposed
in our own instrumentation.
\end{enumerate}

\section{Related work}

\textbf{Repurposing at scale.} Graph-based models predict drug--disease
associations across large biomedical knowledge graphs \citep{txgnn}. These
systems optimise coverage and accuracy. Provenance and calibrated refusal fall
outside their objective, and their outputs are designed for use in place of
independent re-derivation.

\textbf{Selective prediction.} The option to abstain has a long history in
classification, from the reject-option formulation \citep{chow} through
selective classification with coverage guarantees \citep{elyaniv,geifman}.
That literature triggers abstention from model confidence. Our abstentions
follow from a failed precondition on the input record, which makes them
checkable, deterministic, and reportable with a reason.

\textbf{Provenance and tamper-evidence.} We adopt the Merkle audit-tree
construction of Certificate Transparency \citep{rfc6962}, which supplies
inclusion proofs and exposes any post-hoc edit.

\textbf{Missing data.} Rubin's taxonomy \citep{rubin} describes the defect we
found in our own acquisition stage. The loss was \emph{missing not at random},
since the probability of a record being dropped followed from its own size,
which in turn followed from its class.

\input{sections/approach}

\section{Evaluation}
\label{sec:eval}

\subsection{Setup and metric}

We compare two pipelines over identical inputs. \textbf{Enforcement on} is the
system as described. \textbf{Enforcement off} is the same system with one
guard removed, which is the implementation obtained by following each source's
documentation and anticipating no specific failure. Every other guard stays
active in both conditions.

We report the \emph{silent error rate}: the fraction of outputs the unguarded
pipeline emits that are demonstrably wrong. Loud failures, such as a crash or
an out-of-range index, fall outside the numerator, since a downstream consumer
sees them arrive. Absolute counts accompany every rate, because the
denominators here are small enough that a percentage alone would misrepresent
them.

The corpus consists of pathogenic and likely-pathogenic clinical variant
records for \NumGenes{} genes, captured \CorpusDate{}, with subset digest
\texttt{\CorpusDigest{}}. Acquisition reconciles: \CorpusFetched{} records
fetched $=$ \CorpusKept{} admitted $+$ \CorpusExcluded{} excluded $+$
\CorpusUnavailable{} unavailable.

\subsection{Results}

\begin{table}[h]
\centering
\caption{Silent errors prevented. Rates are over what the unguarded pipeline
emits, with loud failures excluded from the numerator.}
\label{tab:ablation}
\AblationTable
\end{table}

Table~\ref{tab:ablation} gives the headline result. Removing G1 makes
\GOneWrong{} of \GOneNaive{} emitted sequences wrong. Each one is valid FASTA
built from real residues, and a structure-prediction tool accepts it without
complaint and returns a structure for a protein that has never existed. With
G1 active the lane emits \GOneEmits{} sequences, \GOneMissense{} substitutions
and \GOneTruncating{} truncations, and we verified all \GOneEmits{} of them
position by position against their wild-type reference.

Removing G2 attributes \GTwoWrong{} of \GTwoNaive{} records to genes they say
nothing about. The per-gene effect (Table~\ref{tab:pergene}) varies sharply,
and this variation carries the more interesting result. \GTwoZeroed{} of
\NumGenes{} genes (\GTwoZeroedNames{}) fall from apparent evidentiary support
to an honest zero. The unguarded pipeline reports all three as supported, and
for one of them the top-ranked supporting evidence is a chromosomal deletion
syndrome.

\begin{table}[h]
\centering
\caption{Per-gene effect of G2. $^{*}$ marks genes reduced to zero.}
\label{tab:pergene}
\PerGeneTable
\end{table}

\subsection{The cost of abstention}

A trivial policy that declines everything also achieves a zero silent error
rate, and it is useless. The guards therefore have to decline
\emph{selectively}. The sequence lane declines \SeqDeclined{} of
\GOneProcessed{} records (Table~\ref{tab:decline}). Most declines are records
carrying no protein-level change, or frameshifts whose downstream sequence the
record leaves undetermined, and a substituting pipeline would emit neither
class. Among the records that are genuinely substitution-eligible, the guard
declines \GOneWrong{} and emits \GOneEmits{}, all verified correct.

\begin{table}[h]
\centering
\caption{Why the sequence lane declines. Only \emph{residue mismatch} covers
records that a substituting pipeline would otherwise have emitted.}
\label{tab:decline}
\DeclineTable
\end{table}

\section{Defects found in our own instrumentation}
\label{sec:audit}

We applied the protocol to our own pipeline before submission. Two findings
bear directly on the results above, and we report them here because leaving
them out would reproduce the failure this paper describes.

\textbf{The acquisition stage discarded records without counting them.} A
reconciliation check requires fetched records to equal admitted plus excluded
plus unavailable. That check revealed 100 records leaving through a single
unlogged branch. The cause lay upstream: the source declines to serialise a
response above a fixed size, and one record listing over a thousand genes
carried its whole batch past that limit. Our error handler read three possible
error keys, and the source reports this condition under a fourth, so our own
log recorded a generic ``no result'' string. \emph{We had built a guard that
detected the loss and discarded its explanation.} Under Rubin's taxonomy
\citep{rubin} the loss was \emph{missing not at random}: the records we lost
were large, their size followed from spanning many genes, and spanning many
genes is exactly the class G2 exists to exclude. Left uncorrected it biased
our own G2 rate downward by roughly six percentage points.

\textbf{Our first reported rate was wrong by ten points.} An independent
recount disagreed with the pipeline's own count of G1 rejections. The pipeline
tested the residue precondition before classifying the variant, so frameshift
records that also mismatched were filed as residue mismatches, which inflated
the reported figure with records that a substituting pipeline could never
emit. The published rate was 24.6\%, and the correct rate is \GOneRate{}. The
emitted sequences were byte-identical before and after the fix, since the
guard had rejected exactly the same records and only the reasons were wrong.
The defect lay entirely in the measurement, which is why the system looked
healthy and why recounting was the only thing that exposed it.

We draw one narrow conclusion. Mechanical reconciliation, asserted at every
boundary, caught defects that careful reading missed, including defects in the
instrument that produces this paper's numbers.

\section{Limitations}

\textbf{The G1 effect comes from one gene.} All \GOneWrong{} residue
mismatches occur in a single gene. A second gene contributes more emitted
sequences than that one and produces zero. The measured G1 result therefore
reflects one transcript-numbering convention, and $n=1$ describes it more
honestly than $n=\NumGenes{}$.

\textbf{The declined records are recoverable, and we discard them.} We tested
the failing records directly and found that a single constant offset of $+30$
residues reconciles all \GOneWrong{} of them exactly. They are correctly
numbered against a longer transcript, and they become wrong only on contact
with the canonical sequence. Abstention is the safe response here. A pipeline
that detected the offset would recover them, and ours throws them away.

\textbf{Small denominators and a single corpus.} The rates come from hundreds
of records, drawn from one snapshot of one source. That source is live, and
two honest runs a day apart returned different totals, which is why we pin a
capture date and publish the digest of the exact table used.

\textbf{We evaluate our own guards on data we selected.} We designed the
guards, and the failures they catch are failures we anticipated. This measures
the value of enforcement given a correct precondition. It leaves open how many
preconditions we failed to think of, and Section~\ref{sec:audit} shows that
this residual exceeds zero.

\textbf{We do not evaluate the science.} Establishing whether any proposed
pairing is biologically correct requires wet-lab validation we have not
performed. The system's claim ceiling is enforced in code and permits a
repurposing hypothesis, stopping short of any therapeutic claim.

\section{Conclusion}

Provenance and abstention are usually argued from principle. We argue for them
from a measurement: on real public records, removing two verification guards
causes a pipeline to emit \GOneWrong{} of \GOneNaive{} silently wrong
sequences and to misattribute \GTwoWrong{} of \GTwoNaive{} variant records.
The intervention is architectural, so it transfers to any pipeline that
assembles evidence into claims, whether or not a language model participates.
The most persuasive evidence we can offer is that the discipline caught our
own instrument twice, and that a mechanical check was what caught it each
time.

Two directions follow. Custody can extend to the turn level of the model
interactions that assist the pipeline, so that the assistance itself carries
receipts. The transcript-offset records can be recovered in place of being
declined.

\section*{Reproducibility statement}

Every figure in this paper is generated from the pipeline outputs by a script,
and no number is typed by hand. A second script scans the prose for any figure
that a later run has superseded. An anonymised artifact containing the code,
the captured corpus with digests, and a one-command regeneration of every
table is available to reviewers [link withheld for anonymous review].

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
