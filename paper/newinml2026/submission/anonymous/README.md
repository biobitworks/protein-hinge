# Anonymous Review Package

This is a deliberately minimal double-blind verification subset. The final seal workflow adds the exact submitted PDF and runtime receipts after validation. The included methods exercise the deterministic G3 normalization/guard contract; they do not claim to reproduce the unrecovered historical G1/G2 corpora or every experiment reported in the paper.
