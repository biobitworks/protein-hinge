# Open questions for the team

These are **not** new experiments. They are operator / team decisions.

1. **PR #1 merge policy.** Keep OPEN_HASH_ADMITTED_ONLY, or import admitted bytes into `main` so a clone without the fork can replay G1/G2? Do not merge the Elvis silent-error manuscript as-is.

2. **Stale authority file.** Update or annotate `paper/newinml2026/provenance/CANONICAL_PAPER_SOURCE.yaml` (`2ba0d923`) so it cannot be mistaken for live CI source `bc4b0d5`.

3. **Stale experiment indexes.** `EXPERIMENT_MATRIX.md`, handoff `05_EXPERIMENT_INDEX_ML.jsonl`, and THESIS-001 `unresolved_blockers` still contradict successor receipts.

4. **SOT-008 +30.** Remains NOT_ESTABLISHED. Either supply row-level proof or keep it out of any camera-ready text.

5. **SOT-020 contributor roster / OpenReview metadata.** Still OPERATOR_INFORMATION_REQUIRED for final submission seal.

6. **Table generator.** Canonical Table 1 matches receipts but is hand-authored LaTeX. Team may accept YELLOW or later wire a generator **without** importing Elvis silent-error macros.

7. **206 MB bootstrap.** Re-hash the admitted zip if the 206 MB prose must be FULLY closed (PARTIAL_TRACE in this session).

8. **Live Elvis `main.tex` vs admitted snapshot.** Live `d0e992be` added `\date{}`. Confirm no further scientific deltas before any future hash re-admission.
